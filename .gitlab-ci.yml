image: node:latest

stages:
    - test
    - pages

test:
    stage: test
    script:
        - npm install --silent
        - npm test

pages:
    stage: pages
    script:
        - echo 'Nothing to do...'
    artifacts:
        paths:
            - public
    only:
        - master
