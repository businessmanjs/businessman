image: node:latest

stages:
    - test
    - pages

test:
    stage: test
    script:
        - npm install --silent
        - npm test
    only:
        - develop

pages:
    stage: pages
    script:
        - echo 'Nothing to do...'
    artifacts:
        paths:
            - public
    only:
        - master
